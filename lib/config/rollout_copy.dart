/// User-visible copy for rollout / service-area gates (keep in sync with backend).
class RolloutCopy {
  RolloutCopy._();

  static const String notAvailableInArea =
      'NexRide is not available in your area yet.';
}
